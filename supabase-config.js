// Add the public values from Supabase Project Settings > API.
// The anon key is safe to ship in this browser application when RLS is enabled.
window.WORDHIVE_SUPABASE = {
  url: "",
  anonKey: ""
};
